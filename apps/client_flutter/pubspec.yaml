name: client_flutter
description: Multi-Platform IPTV Engine App (Android TV, tvOS, Mobile, Desktop).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  video_player: ^2.9.1
  parser_engine:
    path: ../../packages/parser_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
